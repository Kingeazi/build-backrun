[project]
name = "build-backrun"
authors = ["BackRun Development Team"]
description = "Decentralized fitness challenge platform"
telemetry = false
requirements = []
[contracts.backrun-challenge]
path = "contracts/backrun-challenge.clar"
depends_on = []

[repl]
costs_version = 2
parser_version = 2

[repl.analysis]
passes = ["check_checker"]

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false
